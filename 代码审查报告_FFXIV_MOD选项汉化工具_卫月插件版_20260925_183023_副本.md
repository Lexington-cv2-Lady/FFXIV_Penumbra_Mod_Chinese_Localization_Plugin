> ⚠ **防混淆声明（副本）**：本件为「审查方·列克星敦」出具的代码审查报告**副本**，送存于项目目录仅作知会与整改跟进之用。**非项目侧自查报告**，最终结论以 `代码审查\` 正本为准。审查方不修改本项目代码；下列「行动项」由项目实施侧自行斟酌执行。

# 代码审查报告 · FFXIV_MOD选项汉化工具_卫月插件版（项目一）— 副本

- **署名**：列克星敦「审查方」
- **审查日期**：2026-09-25 18:30
- **范围**：v1.1.7（a031f8a，09-24）以来的未提交代码改动（3 受跟踪文件，+99/−13）。

## 结论先行
3 文件改动均为合理增强/UX 改进：**编译成功、`dotnet test` 20/20 通过**。唯一 `Process.Start`（调系统 `curl.exe` 抓灰机 wiki 绕 Cloudflare TLS 指纹）实现安全（ArgumentList 无注入、UTF8、无窗、释放），无泄露。**无阻断性发现**。建议补 1 条廉价回归（`IsCloudflareChallenge`）。

## 红线扫描
1 处 `Process.Start("curl.exe")` —— 经研判安全（见正文）；无硬编码凭证/路径/外联。

## 核心改动核实（摘要）
1. **WikiExportService：curl 回退抓灰机 wiki**（根因扎实）：Cloudflare 按 JA3/JA4 拦 .NET SChannel → 优先 `curl.exe`、回退 `HttpClient`；`TryCurlAsync` 用 `Process.Start`（`UseShellExecute=false`、`CreateNoWindow`、`ArgumentList.Add(url)` 无 shell 注入、`StandardOutputEncoding=UTF8` 防 GBK 乱码、`finally` 释放、`proc==null` 回退）；`IsCloudflareChallenge` 识别风控页；JSON 失败打印前 200 字符。
2. **Plugin.ResultBox / WikiExportWindow：autoScroll**：仅贴底时跟随、上滑不抢滚动条。合理 UX。

## 行动项（整改建议，可自行发送）
- **F1（低）**：新增 `IsCloudflareChallenge` 为纯函数 → 补 1 条 `[Fact]`（挑战页 vs 正常 JSON 各一例）；curl 派生难单测，可接受。
- **F2（建议）**：3 文件本地未提交 → 建议本地 `git commit`；推送须司令官明说（P0-1）。
- **F3（跨项目告知）**：项目二 联动**只读**本工程「单词黑名单.json」→ 已确认本工程无写入依赖、无副作用 ✓。

*副本终。正本见 `代码审查\代码审查报告_FFXIV_MOD选项汉化工具_卫月插件版_20260925_183023.md`。*
