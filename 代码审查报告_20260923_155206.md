# 代码审查报告（卫月插件版 V1.1.5）

- **项目**：FFXIV_MOD选项汉化工具_卫月插件版（卫月插件 / Penumbra 模组汉化）
- **版本**：V1.1.5（最新本地提交 `d4123aa` = Penumbra 1.7.2.2 适配，未推送）
- **审查日期**：2026-09-23 15:52（本地）
- **审查范围**：`FFXIV_Penumbra_Mod_Chinese_Localization_Plugin` 全部源码 `.cs`（Services 14 + Windows 13 + Plugin/Configuration 2 + 共用类型 `ModFileInfo` 等）+ 项目 `工作记忆.md` 审查指南
- **审查依据**：`工作记忆.md` 6.1-6.6（职责边界 / 数据流 / 不变量 / 已知接受项 / 验证方法 / 报告格式）+ 10 条不变量清单
- **编译状态**：已前置验证（`DALAMUD_HOME` + `dotnet build -c Debug`，0 警告 0 错误）
- **总体结论**：数据 / 文件安全层（不变量①-⑤⑦）基本达标，且历史待决项（词典非原子写、静态字典无锁、损坏静默清空）已闭环；UI 层（⑧⑨⑩）达标；**发现 1 处"文档 vs 代码"优先级分歧（中危，待司令官拍板）**，外加 2 处低风险一致性观察。

---

## 一、新发现 / 待定项

### [中危] 词典查询顺序使"我的翻译"压过"个性翻译"，与文档既定优先级相悖

- **现象**：全部词典查询入口都先查 `_mods`（"我的翻译"的 per-mod 精确层），再查 `_terms`（含"个性翻译"的覆盖层），导致"我的翻译"的逐模组词条会盖掉"个性翻译"。
- **证据**：
  - `Services/ImportService.cs:217-218` — `LookupMod($"{fileName}||{field}||{english}")` 先查；`if (zh == null) zh = LookupTerm(english)` 后查。
  - `Services/Translator.cs:33-41` — 同序：`LookupMod` 在前，`LookupTerm` 在后。
  - `Services/MarkService.cs:154-155` — `AwaitsTranslation` 同样先 `LookupMod` 后 `LookupTerm`。
- **层映射（已通读 `DictionaryService.cs` 确认）**：
  - `_mods`（L19、L86-87）= `我的翻译.json` 的 **mods 双层** + `AI知识库` 的 mods 段 → 属于"我的翻译"范畴。
  - `_terms`（L17-18、L90-94）= `我的翻译` 的 terms + `个性翻译.json`（L70-71，**后加载、覆盖了前者同键词条**）+ wiki + AI。
- **与文档冲突**：`DictionaryService.cs:12` 类注释与 `工作记忆.md` P3-1 既定优先级均为 **单词黑名单 > 个性翻译 > 我的翻译 > wiki 术语**。即"个性翻译"本应是覆盖层、优先级最高；但代码把 `_mods`（"我的翻译"）查在 `_terms`（含"个性翻译"）**之前**，于是：
  - 若某英文词**同时**有"我的翻译"的 per-mod 词条与"个性翻译"的全局覆盖词条 → 代码采用"我的翻译"，**忽略"个性翻译"**。
- **用户可见影响**：用户在"个性翻译"里精心设的全局覆盖（例如希望某词统一译法）会被该模组的"我的翻译"精确词条压住，结果不符合文档承诺的优先级。属"文档 vs 代码"语义分歧，**非代码自相矛盾**，不影响数据安全。
- **处置建议（待司令官拍板）**：
  1. 若"精确 per-mod 优先于全局覆盖"是**预期设计**（更精确者胜出）→ 仅需把文档 P3-1 / 类注释改为"我的翻译(mod 精确层) > 个性翻译 > 我的翻译(terms) > wiki"，消除文档与代码不一致；
  2. 若应严格遵循文档"个性翻译 > 我的翻译"→ 需调整查询顺序：先 `LookupTerm`（含个性翻译）后 `LookupMod`（mods），或把"个性翻译"从 `_terms` 拆出独立最高优先级层，先查它。
  - 妾身按审查指南 6.6 先说明影响，**不擅自改动**，等候司令官定夺后再执行。

### [低危] `AI知识库` 的 mods 段未受 `overlayOnly` 保护，可能覆盖"我的翻译"

- **现象**：`DictionaryService.LoadMergedDict` 对 `AI知识库` 传 `overlayOnly:true`，但其保护只作用于 `terms` 合并（`L222-224`、`L251-252`）；`mods` 段（L127-154）**无条件**执行 `_mods[key] = zhS`。
- **证据**：`Services/DictionaryService.cs:76-78` 注释声明"AI 知识库…优先级低于 我的翻译/个性翻译"，但 L127-154 的 mods 写入无 `overlayOnly` 短路；又因 `AI知识库` 在 `我的翻译` 之后加载（L78 晚于 L67），同键 mods 条目会被 AI 覆盖。
- **影响**：仅当 `AI知识库` 与"我的翻译"存在**完全相同的 per-mod 键**（`文件名||字段||原文`）时才触发，概率低；但违背了"AI 低于我的翻译"的注释承诺，是一致性瑕疵。
- **建议**：在 mods 段（L127 起）对 `overlayOnly` 同样做短路：`if (overlayOnly && _mods.ContainsKey(key)) continue;`。可作为上述中危项一并处理。

### [低危] 详情区"保存修改"写回前未校验备份是否成功

- **现象**：`MainWindow.SaveEdits` 调用 `CreateModZip` 后**未判空**，备份失败时会继续写回；而 `ImportService.ApplyDictionary`（L133-138）与 `ModRestoreService` 的 `BackupNow`（L1458-1462）在备份失败时会**中止**写回。
- **证据**：`Windows/MainWindow.cs:1552` — `plugin.Backup.CreateModZip(modDirPath, plugin.Configuration.BackupCount);`（结果未使用、未校验），随后仍执行 `JsonFile.WriteAtomic`（L1633）。
- **影响**：无数据损坏风险（`WriteAtomic` 保证原子写、且文件仅在解析成功后改动），但"写回前必备份"这一安全网在该路径上弱于其它路径。属一致性观察，非缺陷。
- **建议**：与 `ImportService` 对齐——若 `CreateModZip` 返回 `null`，提示"备份失败，已中止保存"并 `return`。

---

## 二、已知接受项（已与文档 / 历史确认，非缺陷，不计入问题）

| 项 | 说明 | 位置 |
|---|---|---|
| 词典非原子写 | 已闭环：统一走 `JsonFile.WriteAtomic`（.tmp→`File.Move`，UTF8 无 BOM）。`SumupService.SaveDict`、`ModFileService.WriteTranslation`、`MainWindow.SaveEdits`、`ImportFromClipboard` 均调用 | `Services/JsonFile.cs:WriteAtomic` |
| 静态字典无锁 | 已闭环：`AiTranslateService._maxTokensLearned` 改为 `ConcurrentDictionary<string,long>`，跨线程无锁安全 | `Services/AiTranslateService.cs:643` |
| 损坏静默清空 | 已闭环：`SumupService.LoadDict` 解析失败 `throw new InvalidDataException`，由调用方拒绝写回（不再静默清空） | `Services/SumupService.cs:110-119` |
| `TextListFile` 损坏静默跳过 | 已知接受：BOM 兼容、`#` 注释、逗号分隔，单条损坏仅跳过该条 | `Services/TextListFile.cs` |
| `AppLog.LastError` 单条 | 已知接受：环形缓冲 2000 条 + 文件落盘，`LastError` 仅留最近一条供 UI 提示 | `Services/AppLog.cs` |
| `FileDialogManager` 无 Dispose | 已知接受：本版未实现 `IDisposable`，无句柄泄漏风险（Dalamud 托管） | `Windows/DictionaryWindow.cs` |
| `ReadModFiles` 跳过乱码文件名 | 已知接受：含 `\uFFFD` 或代理区字符的文件名视为编码损坏副本主动跳过，避免读写/备份污染 | `Services/ModFileService.cs:33-40` |
| `CleanupLegacyBak` 递归删旧备份 | 已知接受（低风险）：启动时清理插件自身遗留的 `*.json.bak/.bak2` 及时间戳备份，penumbra 根递归、其余目录仅顶层；仅删插件备份约定文件 | `Services/ModFileService.cs:138-171` |

---

## 三、10 条不变量逐条核验

| # | 不变量 | 结论 | 关键证据 |
|---|---|---|---|
| ① | 写回前备份 | ✅ 达标 | `ImportService.cs:131-141`（整个模组 zip 备份）、`MainWindow.cs:1552`、`ModRestoreService.BackupNow:1458` |
| ② | 写回后重载传显示名 | ✅ 达标 | `ImportService.cs:158-161`（`Reload(kv.Key, kv.Value)` 传目录+显示名）、`BackupManager.Restore` 后 `Reload` |
| ③ | 纯英文才存快照 | ✅ 达标 | `EnglishSnapshotService.cs:40-55`（`ContainsChinese` 则 `return false`） |
| ④ | meta 双格式兼容 | ✅ 达标 | `ModFileService.ParseMeta:57-64` / `WriteTranslation:223-225`（顶层 Groups 与 `Mod.Groups` 包装同规则解析写回） |
| ⑤ | 还原保留选项状态 | ✅ 达标 | `BackupManager.MergeMetaKeepState:247-278`（仅覆盖 Groups、保留当前 Meta 节点）；`ModRestoreService.RestoreFromPmp` 同思路 |
| ⑥ | 删除走回收站 | ✅ 达标 | `BackupManager.Delete:151` → `SendToRecycleBin` |
| ⑦ | 长任务可取消 | ✅ 达标 | `AiTranslateService.TranslateAsync:297`（`CancellationToken` 透传至 `Http.SendAsync`/`ReadAsStringAsync`）；`MainWindow` 停止按钮 `_ocStopRequested` 防重复；取消后已完成批次照常写盘 |
| ⑧ | 本插件文案免疫自身替换 | ✅ 达标 | 翻译管线只作用于外部模组文件（meta.json / group_*.json），无任何路径触及插件自身 UI 字符串 |
| ⑨ | ImGui `##` 区分 ID | ✅ 达标 | 全局 `##` 前缀防撞车：`##ModSearch`/`##sel{i}`/`##{i}`/`##file`/`##openModFolder`/`##MainResult`（MainWindow）；`全选##step1`/`全选##step5`（TranslatePipelineWindow）；`##g{g.Index}`/`##OptEditScroll`（OptionEditWindow） |
| ⑩ | UI 状态整串赋值 | ✅ 达标 | 后台任务对 `_ocStatus`/`_restoreStatus` 仅做整串赋值（`MainWindow.cs:62,67,964-1024` 等），UI 每帧轮询读取，无读改写复合操作 |

---

## 四、Penumbra 集成与标记自愈（1.7.2.2 适配）确认

- **标记自愈**：`MarkService.PruneStaleMarks` / `IsStale`（L98-141）正确实现"有标记但内容被 Penumbra 升级/重下/手动替换还原成英文 → 判失效并清除标记"，并在 `Plugin.StartAutoHanhua`（MainWindow L1065）调用，使失效模组重新进入自动汉化。✅
- **Reload 日志**：重载操作均带目录+显示名入参与日志，符合②。✅
- **写回 meta 原子写**：`WriteTranslation` 经 `JsonFile.WriteAtomic`，符合 1.7.2.2 适配意图。✅

---

## 五、结论与下一步

- 安全与稳定性：**达标**，历史待决项已全部闭环，可视为本版（d4123aa）具备推送条件（建议先解决下方待定项）。
- **待司令官拍板（中危）**：词典优先级"我的翻译 vs 个性翻译"的语义分歧——是维持代码现状（精确 per-mod 优先）并改文档，还是严格按文档改查询顺序？妾身已锁定证据，**已于第六节按方案乙闭环（代码贴合文档：个性翻译 > 我的翻译）**。
- 低风险观察（② AI 知识库 mods 覆盖、③ 保存前备份校验）建议与中危项一并处理。

---

## 六、修正实施记录（2026-09-23 约 16:33）

### 决策
司令官授权"自己看着来"，妾身选**方案乙**：让代码贴合文档既定优先级（个性翻译 > 我的翻译），而非改文档迁就代码——文档契约（`DictionaryService.cs:12` 类注释 + `工作记忆.md` P3-1）本就如此声明，代码此前是疏漏。

### 备份（先留后路）
- `备份_卫月插件源码_20260923_162542.zip`（41 个源文件，约 1.1 MB，项目根目录，已排除可再生的 obj/bin）。

### 代码改动（5 个文件 / 6 处）
1. `Services/DictionaryService.cs`：新增独立最高覆盖层 `_custom`/`_customLower` 与 `LookupCustom`；`LoadCustomOverlay` 改写入 `_custom`；`Load` 中清空新层；`mods` 段新增 `overlayOnly` 守卫（AI 知识库不再覆盖"我的翻译"的 mods 精确词条，顺带闭环观察项②）。
2. `Services/ImportService.cs` `ApplyLookup`：先 `LookupCustom`（个性翻译），后 `LookupMod` → `LookupTerm`。
3. `Services/Translator.cs` `Translate`：新增 step 0 先 `LookupCustom`；`TranslateSubstring` 词块匹配亦先查 `LookupCustom`（与原文行为保持一致）。
4. `Services/MarkService.cs` `AwaitsTranslation`：先 `LookupCustom`，后 `LookupMod` → `LookupTerm`。
5. `Windows/MainWindow.cs` `SaveEdits`：写回前校验 `CreateModZip` 返回值，为 null 时中止保存并提示（与导入/还原路径一致，闭环观察项③）。

### 编译验证
- `DALAMUD_HOME=<XIVLauncherCN\addon\Hooks\dev> dotnet build -c Debug` → **0 警告 0 错误**，已生成并部署到 devPlugins。

### 结语
原报告"未对源码做任何改动"一句已作废：本次已按方案乙修正并编译通过。若司令官更倾向方案甲（维持代码、改文档），妾身可一键回退到备份 zip 再执行。

---

## 七、新增功能：Penumbra 更新后自动重覆盖译文（指令F，2026-09-23 约 20:14）

### 背景与需求
司令官指出 HS 类界面模组（如 AETHERIS UI AIO）被 Penumbra 更新/重下后会把已汉化的中文还原成英文。根因：Penumbra 更新直接覆盖模组文件，译文寄存在独立词典目录不受影响，故可离线用词典预填恢复。司令官要求：检测 Penumbra 更新后自动用词典预填覆盖一次；做成可取消勾选开关（最终由妾身决定：**保留开关、默认开启**）；且覆盖只恢复译文文本，绝不能把已选好的选项（启用/选择）状态重置回默认。

### 关键地基结论（已源码核实）
- **Penumbra 无"模组更新/文件被覆盖"专用事件**：`PenumbraService.cs` 仅订阅 `ModAddedEvent`/`ModDeleted`/`PenumbraDisposed`，本插件 `ModsChanged` 仅由 `Refresh()` 触发（连上/增删时）。故"检测更新"必须自建。
- **写回只改文本、不动选项状态（源码兜底）**：`ModFileService.WriteTranslation`（L212-275）通篇只写 `Name`/`Description` 文本字段，绝不触碰选项 `Settings`（启用/优先级/单选状态）。"不重置选项"是物理保证，非靠约定。
- **离线预填现成**：`ImportService.ApplyDictionary(..., overwrite:false)` 离线（不调 AI、不联网），仅把"当前英文且词典可命中"的条目填回中文；已中文/无匹配跳过。

### 实现方案
- **检测**：插件内建内存基线 `Dictionary<string,(long mtime,long size)>`，记录各模组 `meta.json`+`group_*.json` 的 mtime 与 size 之和。复用每帧 `AutoHanhuaTick()` 心搏，节流 8 秒调用 `DetectAndReHanhua()`：签名未变跳过；变了则只读预扫 `HasRecoverableText`（是否有"非中文/非黑名单/含英文/词典可命中"条目）；有则对该模组离线 `ApplyDictionary`（建 zip 备份→只改文本→重载→同步基线），无则仅同步基线（如用户改选项导致 meta 重写）。写回后基线同步，避免反复触发。
- **开关**：`Configuration.AutoReHanhuaOnUpdate`（默认 `true`），照搬 `AutoHanhuaOnStart/OnNewMod` 范式；主窗口"全自动汉化"区段新增勾选框"Penumbra 更新后自动重覆盖译文"，附 tooltip 说明只改文本不碰选项。
- **复用**：`ApplyDictionary` 与 `WriteTranslation` 均为成熟路径，本功能不新增任何写回逻辑。

### 代码改动（4 个文件）
1. `Configuration.cs`：新增 `public bool AutoReHanhuaOnUpdate { get; set; } = true;`（置于 `AutoHanhuaAfterRestore` 之后）。
2. `Services/ImportService.cs`：新增 `public static bool CanTranslate(...)`（判断某英文条目是否会被词典命中），`ApplyLookup` 改为调用它（行为不变）；供探测预扫复用，避免重复写回逻辑。
3. `Plugin.cs`：新增字段 `_modSig`/`_lastReHanhuaPoll`/`ReHanhuaPollSeconds`；新增 `DetectAndReHanhua()`/`ComputeModSig()`/`HasRecoverableText()`；在 `AutoHanhuaTick()` 末尾挂接 8 秒节流探测；新增 `using System.Collections.Generic;`。
4. `Windows/MainWindow.cs`：`DrawOneClickAll()` 全自动区段新增勾选框 + tooltip。

### 备份与编译
- 备份：`备份_卫月插件源码_20260923_201426.zip`（41 文件，约 1.1 MB，含本次改动前的方案乙源码）。
- 编译：`DALAMUD_HOME=<XIVLauncherCN\addon\Hooks\dev> dotnet build -c Release` → **0 警告 0 错误**。

### 行为边界（已核实）
- 只恢复"词典已知"的译文；从未翻译过的模组（词典无匹配）不会被碰。
- 只改 Name/Description 文本；选项启用/选择状态由 `WriteTranslation` 源码保证不动。
- 默认开启，可随时在设置里取消；逻辑与现有全自动汉化完全隔离，不会搞崩插件。
